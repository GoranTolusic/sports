{
  "name": "expresstype",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "ts-node index.ts",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/index.js\"",
    "installApp": "rm -rf node_modules/ && rm -rf package-lock.json && npm i && npm run createDb && npm run migrate && npm run seed",
    "createDb": "./node_modules/.bin/ts-node createDb.ts",
    "migrate": "./node_modules/.bin/knex migrate:latest",
    "seed": "./node_modules/.bin/knex seed:run",
    "dockerInstall": "npm run createDb && npm run migrate && npm run seed",
    "updateApp": "rm -rf node_modules/ && rm -rf package-lock.json && npm i && knex migrate:latest",
    "dockerize": " npm run dockerImage && npm run dockerRun ",
    "dockerImage": "docker build -t sports --network=host .",
    "dockerRun": "docker run --network=host sports"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.11",
    "knex": "^2.3.0",
    "lodash": "^4.17.21",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4",
    "ts-node": "10.7.0",
    "@types/bcrypt": "^5.0.0"
  }
}
